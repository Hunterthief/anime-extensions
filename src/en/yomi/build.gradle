ext {
    extName = 'Yomi'
    extClass = '.Yomi'
    extVersionCode = 1
}

apply plugin: "kei.plugins.extension.legacy"

dependencies {
    implementation(project(':lib:playlistutils'))
    // Removed cloudflareinterceptor to test raw connectivity
}
